<?php
/**
 * Optional local overrides — copy to mail-config.local.php (not committed).
 *
 * Only needed if Google SMTP relay is not whitelisted for your server IP (e.g. local XAMPP).
 * Generate a Google App Password for marketing@bisanibrother.com and paste below:
 *
 * return [
 *     'smtp' => [
 *         'host'       => 'smtp.gmail.com',
 *         'port'       => 587,
 *         'encryption' => 'tls',
 *         'username'   => 'marketing@bisanibrother.com',
 *         'password'   => 'xxxx xxxx xxxx xxxx',
 *     ],
 * ];
 */
